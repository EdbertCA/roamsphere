<% layout('/layout/boilerplate') %>
<div class="d-flex justify-content-between align-items-center">
  <h1>All <%= foundUser.username %> Listings</h1>
  <small class="text-muted fw-semibold"><%= page %> of <%= totalPages %> results</small>
</div>

<div class="row g-4 mt-0 mb-3">
  <% for (let campground of campgrounds) {%>
    <%- include('../partials/campgroundCard', { campground, favoriteIds }) %>
  <% } %>
</div>

<% 
  const startPage = Math.max(1, page - 2);
  const endPage = Math.min(totalPages, page + 2);
%>

<nav aria-label="Page navigation">
  <ul class="pagination justify-content-center">
    <li class="page-item <%= page <= 1 ? 'disabled' : '' %>">
      <a class="page-link" href="/users/<%= foundUser._id%>/campgrounds?page=<%= page - 1 %>">
        Previous
      </a>
    </li>
    <% for (let i = startPage; i <= endPage; i++) {%>
        <li class="page-item <%= page === i  ? 'active' : '' %>">
          <a class="page-link" href="/users/<%= foundUser._id%>/campgrounds?page=<%= i %>"><%= i %></a>
        </li>
    <% } %>
    <li class="page-item <%= page >= totalPages ? 'disabled' : '' %>">
      <a class="page-link" href="/users/<%= foundUser._id%>/campgrounds?page=<%= page + 1 %>">
        Next
      </a>
    </li>
  </ul>
</nav>